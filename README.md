Install the sandbox
===================

Use composer:

    $ composer create-project le-phare/silex-bootstrap my-great-project


Now configure your prefered web server and make the `web/` directory your document root. That's it !
